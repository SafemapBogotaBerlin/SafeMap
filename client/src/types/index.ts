import { User } from "firebase/auth";
export type RegisterInitData = {
  displayName: string;
};

export type UserData ={
    id:string;
    email:string;
    name:string;
    created:string
}
